# سياق المشروع — اقرأ هذا أولاً

> هذا الملف يحمل سياق المشروع عبر الجلسات. المحادثات لا تُحفظ، لكن هذا الملف يبقى.

## ما هذا

منصة تعليمية لحفظ وضبط نطق الحديث الشريف. طلاب مقرر جامعي في إندونيسيا يسجّلون تلاوتهم، يستمعون لبعضهم، يقيّمون، ويبلّغون عن الأخطاء. المشرف يعتمد التسجيلات الصحيحة.

**غير ربحي · صدقة جارية · مالكه: عبد (Mohammed Rashed A 6105 — جامعة أحمد دهلان)**

## البنية

```
SoT/                          github.com/aplis101/SoT
├── app/                      Next.js 16 + TS + Tailwind (RTL)
│   ├── src/lib/repo/         ← طبقة البيانات: mock | supabase (نقطة التبديل الوحيدة)
│   ├── src/lib/algorithms.ts ← ALG-001..006
│   ├── scripts/              ← استيراد بيانات الحديث
│   └── supabase/seed/        ← 14,650 حديثاً جاهزة (SQL)
├── hadith-sot/               وثائق المصدر الوحيد للحقيقة (v1.1)
├── review/                   6 تقارير نقد — ابدأ بـ00-CRITIQUE-SUMMARY.md
├── docs/PRIVACY-CONSENT.md   الموافقة والخصوصية (UU PDP)
├── DEPLOY.md                 النشر وحفظ المفاتيح
└── origenal-Sot/             النسخة الأصلية قبل النقد (للمقارنة)
```

## القرارات المعمارية المثبَّتة

| القرار | السبب |
|---|---|
| **PWA فقط** — لا تطبيق أصلي | كود واحد ⇒ استحالة تباعد المزايا بين الهاتف والويب. صفر تكلفة. |
| **fawazahmed0/hadith-api** مصدر البيانات | رخصة Unlicense (ملكية عامة)، بلا مفتاح، بلا حدود. عربي مشكّل + إندونيسي. **10 مجموعات = أوسع مصدر مفتوح متاح.** |
| **الغريب/الشرح/التخريج يدخلها المشرف** | لا مصدر مفتوح مرخَّص لها. الدقة العلمية بيد أهلها. `/admin/content` |
| **طبقة `repo`** تفصل الواجهة عن المصدر | التبديل بمتغيّر واحد. لا صفحة تعرف من أين تأتي البيانات. |
| **RLS هو الحارس الأمني** لا كود الواجهة | `anon key` مكشوف بالتصميم؛ الأمان في سياسات قاعدة البيانات. |

## أعراف يجب اتّباعها

- **الواجهة عربية RTL بالكامل.** النص اللاتيني/الإندونيسي يُغلَّف بـ`.latin`، والأرقام بـ`.nums`.
- **لا تكتب أحرفاً عربية داخل ملفات `.bat`** — cmd يفسدها ويكسر الأوامر. اكتشفناها بالتجربة.
- **الإصلاحات المشتقة من النقد مُعلَّمة في الكود** بـ`[FIX <معرّف>]` (مثل `[FIX DB-01]`). لا تحذف هذه التعليقات.
- **تحقّق بالتشغيل الفعلي لا بالقراءة.** خمسة أخطاء جوهرية لم تظهر إلا بتنفيذ حقيقي: ثلاثة P0 صامتة في RLS، وتعارض أسماء أعمدة، و`hadith_number` عشري (402.2) كسر الاستيراد.
- **الوثائق هي المرجع.** أي تغيير سلوكي يُحدَّث في `hadith-sot/` أولاً ثم في الكود.

## الحالة الآن

✅ نقد SoT (69 وثيقة) وإصلاح 8 مشاكل بينها 3 من فئة P0 الصامتة
✅ التطبيق كامل: F001–F008، بُني ونُقر عليه فعلياً في المتصفح
✅ **35,798 حديثاً حيّة على Supabase الحقيقي** (10 مجموعات · 399 كتاباً · 99.7% مترجمة · 67MB)
✅ طبقة Supabase + PWA + لوحة إدخال المحتوى
✅ Supabase: مخطط v1.1 + 30 سياسة RLS + bucket خاص بـ4 سياسات + البذور كاملة
⬜ **Google OAuth** — يحتاج عبد (حساب Google Cloud)
⬜ النشر على Vercel

## قرارات تنتظر عبد

راجع `review/00-CRITIQUE-SUMMARY.md` §القرارات، و`docs/PRIVACY-CONSENT.md` §4. أهمها:

1. ~~هل البريد الجامعي إلزامي؟~~ ✅ **حُسم: لا.** التسجيل مفتوح لأي بريد Google. النية الحالية للزملاء، ومستقبلاً قد تُفتح للعموم كصدقة جارية. نص الموافقة عُدّل ليقول هذا صراحةً.
2. **مصير التسجيلات آخر الفصل** — حذف؟ أرشفة؟
3. **الترجمة الإندونيسية** مجهولة المؤلف في المصدر — تحتاج تحقق قبل النشر العام.

## أوامر سريعة

```bash
cd app && npm install && npm run dev     # تشغيل محلي
node scripts/import-hadith.mjs --all     # إعادة استيراد بيانات الحديث
```

روابط عرض: أضف `?as=student` أو `?as=admin` لأي مسار.

سكربتات جاهزة في الجذر: `run-prototype.bat` · `push-now.bat` · `import-hadith-data.bat` · `_build.bat`
